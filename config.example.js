// Copy this file to config.js and fill in real values.
// config.js is gitignored — it never gets committed.

window.APP_CONFIG = {
    supabaseUrl: "",
    supabaseKey: ""
};

window.WEATHER_LOCATION = {
    lat: 0,
    lon: 0,
    label: "Your City, ST"
};